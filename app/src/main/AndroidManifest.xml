<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Quyền truy cập Internet -->
    <!-- Quyền truy cập Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UngDungChuyenDoiTienTe"
        tools:targetApi="31">
        <activity
            android:name=".BieuDo"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Các activity khác -->
        <activity
            android:name=".Convert"
            android:exported="false" />
        <activity
            android:name=".ChonDonViTienTe"
            android:exported="false" />
        <activity
            android:name=".CaiDat"
            android:exported="false" />
        <activity
            android:name=".DangKy"
            android:exported="false" />
        <activity
            android:name=".Profile"
            android:exported="false" /> <!-- MainActivity sẽ được mở sau SplashActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Không cần intent-filter MAIN và LAUNCHER nữa vì SplashActivity đã xử lý -->
        </activity>
    </application>

</manifest>